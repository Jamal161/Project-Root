ReactJs
NodeJs
ExpressJs
Mongodb
Laravel
MySQL

Steps to install
npm install 
composer install

Steps to run -
`cd frontend`
`npm start`

`cd backend`
`npm run dev`

`cd customer-notify`
`php artisan run`
